# FeiHou Easy H3 人脸精修（实验）

RH 版新增独立节点，不改变现有主节点及工作流。额外执行 H3 局部采样，不是锐化滤镜。

## 安装可选依赖

使用运行 ComfyUI 的 Python：`python -m pip install -r requirements-face-refine.txt`。
检测模型如 `face_yolov8m.pt` 放在 `ComfyUI/models/ultralytics/bbox/`。
参考图身份匹配还需要 InsightFace、合适的 ONNX Runtime 和识别权重；请自行核实模型授权。
不要同时安装 CPU/GPU 两种 ONNX Runtime。依赖缺失只影响此节点，不阻止原有节点启动。

## 接线

参考人脸自动处理：外接 reference_face 优先，否则读取 H3 context 的第一张原始参考 IMAGE。
主节点保留第一张图的 RGB CPU 副本（缩放到参考尺寸前），不是从 latent 反推，也不会加载全部参考图副本。
旧 context 缺少该字段时仍可使用外接参考图；两者都没有时，最大脸模式沿用视频裁剪帧参考，身份匹配模式明确报缺少参考图。
外接/自动参考图片先用相同检测模型及阈值检测人脸，选取面积最大脸，按“裁剪范围”生成方形区域并缩放至精修画布尺寸。
边界不足使用边缘延展，不拉伸变形；多脸图片建议先手动选出所需人物外接。
检测不到人脸时明确报错，不把风景或未识别人脸的全图直接交给采样。批量图片只取第一帧。
此改动不增加界面控件，需重新运行主节点才能获得带原图的新 context；额外占用一张原图的 CPU 内存。

- 最终 VAE 解码的 IMAGE → images（原生 24 FPS，在插帧前精修）。
- 用于精修的 H3 MODEL → model；LoRA 在外部加载，节点不自动加 Turbo。
- Easy H3 主节点的 h3_context → h3_context。
- 可选参考人脸 → reference_face。`reference_identity` 模式用它选人；其他模式用于 H3 身份参考。
- 最终视频的原音频 → audio（不要误接未对齐的参考歌曲）。未接时返回 None，只连接画面输出即可。
- 输出 images/audio/fps → 视频保存节点；report 接文本预览。

默认最大人脸、768 方形裁剪、8 步、denoise 0.25。数值只是试验起点，步数应匹配外接模型/LoRA。
高级选项可改画布、检测阈值、裁剪范围、分段帧数、采样器、调度器和局部提示词。
隐藏高级选项不会重置其保存值。建议提示词只描述面部和保持动作，不复制包含多个镜头的全文。

## 纯净二采模型及界面

高级选项新增“使用纯净二采模型”，默认开启，位于原音频约束之后。
连接 Easy H3 的二采 MODEL 即可：内部提取加载器应用 LoRA 前的基础分支。
不增加主节点接口，不重复从磁盘加载基础权重，但保留共享权重的基础 patcher 记录。
修脸前会卸载当前活动二采模型及克隆，确保普通/旁路 LoRA 被撤销；此必要切换不受强制卸载开关控制。
强制卸载控制的其余采样前后清理保持原逻辑，界面放在高级选项最下面。

纯净分支不继承二采 LoRA，也不继承输出后额外添加的注意力/采样等模型补丁。
不能移除融合进模型文件的 LMS。外部模型或旧运行缓存没有基础记录时明确报错；
重新运行更新后的 Easy H3 加载/主节点，或在高级选项中关闭此功能使用原输入模型。
提示词控件永久隐藏，但原默认文本、已保存的文本仍参与采样；未删除后端参数。
控件保存仍按原序列，并附带按名称保存的数据，避免把界面顺序调整当成参数顺序调整。

## 原音频约束使用

新增 `原音频约束（实验）` 开关，默认关闭，追加在原有控件之后，不改变旧控件保存顺序。
将与输入视频第 0 帧对齐的最终音频接入 `audio`，再打开此开关。不要连接未截取的整首歌曲。
每段按原视频帧位置取音频（包含重叠段），补齐 H3 网格时填静音；音频不足也补静音并在报告提示。
通过音频 VAE 将该片段放进目标音频 latent，设置视频遮罩为 1、音频遮罩为 0，使用当前 ComfyUI H3 原生遮罩和条件时间步。
不修改 ComfyUI 本体，不叠加旧版 NativeAudioLock 的模型补丁，不依赖安装其他音频节点。
最终输出仍是输入的原音频，不输出重新合成的音频。没有音频时，开关关闭可正常运行，开启则明确报错。

先保持已测试的精修强度 0.25，仅打开此开关作同种子对比。高级提示词可补充本片段的准确台词，但不是必须；多段视频不要把全部台词强塞到每段。
音频约束不等于专用口型校正模型，不能保证每个音素准确；低精修强度也可能限制嘴型调整幅度。尚未进行完整 GPU 口型质量验证。

## 边界

- 分段帧数使用“不分段、240、192、120、72”五个选项，默认不分段。
  不分段只取消长度切块，仍保留切镜边界隔离。长镜头整段处理可能增加显存峰值。
  固定值指补齐 H3 网格之前的最大输入帧数；各段仍补到 17k+5 后裁回，并保留 17 帧重叠。
  旧自定义数值向上迁移到最近预设（如 124 → 192）；超过 240 的旧值转为不分段，请检查长视频设置。
- 主节点新增可见 prompt 字符串输入，未连接时用内置提示词；连接时以外部文本为准。
  制作包连线仍按原规则用分镜提示词覆盖。接口追加，不移动旧连线位置。

- 单人物；按切镜分段。长镜头分块精修，17 帧重叠平均融合，接缝仍需检查。
- 补齐到 H3 帧数网格后裁回，不改变输出帧数、尺寸或 FPS。
- 无脸时保留原画面；失踪帧使用上游跟踪器的渐隐回贴保护。
- 关闭音频约束时，原音频仅透传；无论开关状态，都可能改变五官、身份、表情和嘴型。
- 不增加逐帧降噪或注意力补丁；外接 MODEL 上的补丁会被继承。
- 开关关闭/denoise=0 时透传，但 ComfyUI 仍可能计算已连接的上游输入。
- 强制卸载清理检测模型及精修模型/VAE，不能保证任何分辨率都不爆显存。
- 此版为代码/CPU 回归验证原型，尚无完整 GPU 生成质量验证；不要替代原工作流。

## 来源

跟踪及回贴逻辑改编自 https://github.com/Carasibana/ComfyUI-H3-FaceRefine 的 main 源码（2026-09-13 获取）。
保留 MIT 许可于 LICENSE-FaceRefine.txt。仅包含跟踪/回贴模块，未引入原项目网页接口、文件浏览服务或节点注册。

RunningHub must provide the optional Python dependencies and detector weights on its server. This package cannot install platform-side dependencies or bypass platform restrictions.
